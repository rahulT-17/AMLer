# STUB / correlation evaluator : this will handle the correlation policy rules and evaluate them against the data

from .base import BaseEvaluator

class CorrelationEvaluator(BaseEvaluator) :
    async def evaluate(self, transactions) -> list :

        # TODO : implement correlation evaluation logic here
        return []
        